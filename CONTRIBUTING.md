# Contributing

Use focused feature/fix branches from `dev`, keep commits atomic (`type(scope): summary`), and do not merge incomplete scientific work. Run `make format`, `make lint`, and `make test` before review. Do not commit data, weights, caches, or secrets.
