# Data Pipeline

NASA products are ingested into a manifest with metadata, Moon-specific transform, footprint, and GSD. Intersecting observations yield geographically disjoint train/validation/test regions and pixel-space master ground truth.
