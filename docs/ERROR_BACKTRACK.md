# Error Backtrack

Classify failures (environment, data, CRS, shape, dtype, device, model, checkpoint, loss, gradient, numerical, geometry, metric, API, frontend). Check data and coordinate conventions before changing thresholds or hyperparameters. Store investigations in `logs/debug/error_<timestamp>.json`.
