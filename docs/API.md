# API

Planned FastAPI endpoints: `GET /health`, `POST /analyze`, `GET /jobs/{id}`, `GET /jobs/{id}/result`, and `GET /artifacts/{id}`. Failures include stage, code, diagnostics, and recommended next action.
