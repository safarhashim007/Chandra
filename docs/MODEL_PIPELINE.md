# Model Pipeline

RoMa v2 is the baseline. LunarRoMa must reuse official modules via a gradient-enabled path while official inference remains untouched. Model promotion requires held-out improvement at controlled false-accept rate.
